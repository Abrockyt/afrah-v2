process.env.PW_TEST_SCREENSHOT_NO_FONTS_READY='1';const {chromium}=require('C:/Users/abroc/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const fs=require('fs/promises');const assert=require('assert/strict');
(async()=>{await fs.mkdir('qa/mixed',{recursive:true});const b=await chromium.launch({channel:'msedge',headless:true,args:['--enable-webgl','--ignore-gpu-blocklist']});const p=await b.newPage({viewport:{width:1440,height:1000}}),errors=[];p.on('pageerror',e=>errors.push(e.message));p.on('console',m=>{if(m.type()==='error')errors.push(m.text())});await p.goto(process.env.BASE_URL||'http://localhost:4174',{waitUntil:'networkidle'});await p.locator('.era-intro').waitFor({state:'detached',timeout:15000});for(const width of [1440,390]){await p.setViewportSize({width,height:width===390?844:1000});for(const progress of [.1,.7]){await p.evaluate(x=>{const el=document.getElementById('form');scrollTo({top:el.offsetTop+(el.offsetHeight-innerHeight)*x,behavior:'instant'})},progress);await p.waitForTimeout(3500);await p.locator('.model-ready').waitFor({timeout:15000});await p.screenshot({path:`qa/mixed/form-${width}-${progress}.png`});assert.ok(await p.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));}await p.getByRole('button',{name:'DISCOVER THE DETAILS'}).click();await p.waitForTimeout(2200);assert.ok(await p.locator('#materials').evaluate(e=>Math.abs(e.getBoundingClientRect().top)<130));}await p.setViewportSize({width:1440,height:1000});await p.getByRole('button',{name:'VIRTUAL TOUR',exact:true}).click();await p.locator('video').evaluate(v=>{v.muted=true;return v.play()});await p.waitForTimeout(1200);const video=await p.locator('video').evaluate(v=>({duration:v.duration,time:v.currentTime,error:v.error?.message}));assert.equal(video.duration,18);assert.ok(video.time>0);await fs.writeFile('qa/mixed/verification.json',JSON.stringify({errors,video,checks:['Composites GLB rendered in desktop and mobile','Both scroll chapters','Details navigation','No horizontal overflow','18-second film plays']},null,2));await b.close();assert.equal(errors.length,0);console.log('Mixed version visual and film checks passed')})().catch(e=>{console.error(e);process.exit(1)});
