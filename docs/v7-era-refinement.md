# Afrah v7 refinement — 25 September 2026

The active v7 is the base. A reversible source snapshot is in `backups/v7-before-era-refinement-20260925.zip`.

Reference lock: ERA owns the opening composition, navy canvas, fine animated botanical/architectural lines, inset-to-full-frame imagery, large whitespace and separate product pages. Preserve the existing TASA Orbiter / Orbit Display fonts. Likova supplies the current study building; refine its camera and preserve its embedded materials. Silver Pinewood supplies the already-local study sculpture and images; retain a left sculpture/right spatial-gallery composition. The tunnel remains one continuous camera passage.

| Decision | Evidence | Implementation |
| --- | --- | --- |
| New first screen before current scene | User screenshot and live ERA homepage | Split oversized sans type, right-side supporting line, large inset photo, animated fine SVG linework |
| Keep current 3D models | User explicitly accepts models and asks to correct angles | Lower front three-quarter camera; remove unnecessary material overrides |
| Story rhythm and whitespace | ERA homepage architecture/interior/landscape sections | Ivory editorial interlude with asymmetrical photo layouts and route links |
| Spatial history | Live Silver Pinewood lobby and current model | Sculpture on left; photographs advance through depth on right |
| Separate pages | User request; restored v7 route audit | Reconnect existing functional v6 product pages with current font and navy theme |

Referenced study assets remain local previews from the existing archive. Asset source availability is not a license grant; this work does not assert ownership of the reference models or images. No claim of pixel-exact shader equivalence is made.


Implemented refinement: an O-shaped photographic aperture descends after the animated loader, expands to fill the screen, then opens onto the live building. A shared GPU mask covers scene boundaries while the camera rig changes. The tunnel portal fills the frame and the final curtain uncovers the sculpture. History cards revolve on the right without covering the sculpture or headings.

Functional checks: residences filter changes 80 results to 20 two-bedroom results; map landmark selection updates its label; floor selection now uses the same inventory as the residence list (level 15 shows residence 15.B, rather than the nearest level 14). The separate pages retain the current Orbit Display type and navy palette.
