# 20 / Implementation roadmap
1. **Research and concept review (this package).** Evidence, original massing options, proposed design system, storyboard and asset list. Resolve ERA/Whiteley research gaps where practical.
2. **Architecture blockout.** Select one massing, build Blender collections, render day/dusk and hero camera. Review silhouette and scale before façade detail.
3. **Four-scene prototype.** Shell/loader → LAND → FORM → LIGHT. Native headings and controls; persistent model; honest loader; poster fallback. No inner-page build.
4. **Validation.** Desktop/mobile frame timings; keyboard/reduced-motion; building composition and material response; user observation.
5. **After prototype approval.** MATERIAL → ENTER → LIFE → EXPLORE → PLACE → HOME and enquiry; source real content; create Remotion bridges and optional sound.
6. **Integration.** Replace temporary assets, audit licences/data, connect real enquiry service, deploy staging, then approved production integration.

Definition of first prototype done: scene loads progressively; construction is understandable; Day/Dusk changes light without reloading model; skip/replay work; narrow screens remain usable; fallback is present; actual performance evidence is attached.
Keep original checkout unchanged until integration. No production deployment has been performed.
